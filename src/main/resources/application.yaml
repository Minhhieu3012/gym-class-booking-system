spring:
  application:
    name: gym-booking

  datasource:
    url: jdbc:postgresql://localhost:5432/gym_booking_db
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true